<Page
    x:Class="WinUI_Desktop.Clipboard.ImageSelector"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUI_Desktop.Clipboard"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Frame x:Name="ImgSelectorFrame">
        <Grid x:Name="SelectLayout"  Width="Auto" Height="455" Background="Black">
            <Grid.RowDefinitions>
                <RowDefinition Height="400" />
                <RowDefinition Height="50" />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Name="ImagePickerPanel" Background="Black" >

            </StackPanel>

            <StackPanel Grid.Row="1"  Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top" >
                <Button x:Name="OK" Click="TurnTo_ImgCopy">OK</Button>
            </StackPanel>
        </Grid>
    </Frame>
</Page>
